

/*

    this is called in openscout_block.php
    userCourses is a array of courses also defined in the openscout_block.php
        it has course.id and course.fullname
    
    

*/

function logMD(openScout_userid, moodle_userid, action, param, userCourses){

   // alert(openScout_userid +" - "+ moodle_userid +" - "+ action +" - "+ param +" - "+userCourses);

}